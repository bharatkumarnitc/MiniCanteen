<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.minicanteen">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Admin.Admin_Exp_table"></activity>
        <activity android:name=".Admin.Admin_Add_Money" />
        <activity android:name=".Admin.Admin_Profile" />
        <activity android:name=".Admin.Admin_Student_amount" />
        <activity android:name=".Admin.Admin_Page" />
        <activity android:name=".Admin.Admin_Login" />
        <activity android:name=".Admin.Admin_Register" />
        <activity android:name=".Student.Expenses"
            android:parentActivityName=".Student.User_Profile"/>
        <activity android:name=".Student.User_show"
            android:parentActivityName=".Student.User_Profile"/>

        <activity android:name=".Student.Scanner_QR"
            android:parentActivityName=".Student.User_Profile"/>
        <activity android:name=".Student.Signup" />
        <activity android:name=".Student.Otp" />
        <activity android:name=".Student.User_Profile" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>